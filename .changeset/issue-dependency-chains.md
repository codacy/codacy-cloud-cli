---
"@codacy/codacy-cloud-cli": minor
---

Show dependency import chains on `codacy issue`/`codacy issues` for SCA issues, matching what `codacy finding`/`codacy findings` already show. `--output json` gains `dependencyChains` on the issue payload.
